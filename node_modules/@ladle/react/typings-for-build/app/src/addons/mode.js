import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import queryString from "query-string";
import { Preview } from "../icons";
import { ModeState, ActionType } from "../../../shared/types";
import config from "../get-config";
export const getQuery = (locationSearch) => {
    const mode = queryString.parse(locationSearch).mode;
    switch (mode) {
        case ModeState.Full:
            return ModeState.Full;
        case ModeState.Preview:
            return ModeState.Preview;
        default:
            return config.addons.mode.defaultState;
    }
};
export const Button = ({ dispatch }) => {
    const text = `Open fullscreen mode. Can be toggled by pressing ${config.hotkeys.fullscreen.join(" or ")}.`;
    return (_jsx("li", { children: _jsxs("button", { "aria-label": text, title: text, onClick: () => dispatch({ type: ActionType.UpdateMode, value: ModeState.Preview }), type: "button", children: [_jsx(Preview, {}), _jsx("span", { className: "ladle-addon-tooltip", children: text }), _jsx("label", { children: "Open fullscreen mode" })] }) }));
};
