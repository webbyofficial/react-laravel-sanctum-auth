import * as React from "react";
import type { RequestHandler } from "msw";
declare const Msw: ({ children, msw, }: {
    children: React.ReactElement;
    msw: RequestHandler[];
}) => React.ReactElement<any, string | React.JSXElementConstructor<any>> | null;
export default Msw;
