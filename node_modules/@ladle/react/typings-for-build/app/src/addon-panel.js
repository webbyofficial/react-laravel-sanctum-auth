import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Button as ThemeButton } from "./addons/theme";
import { Button as ControlButton } from "./addons/control";
import { Button as ModeButton } from "./addons/mode";
import { Button as ActionButton } from "./addons/action";
import { Button as RtlButton } from "./addons/rtl";
import { Button as SourceButton } from "./addons/source";
import { Button as LadleButton } from "./addons/ladle";
import { Button as A11yButton } from "./addons/a11y";
import { Button as WidthButton } from "./addons/width";
import config from "./get-config";
const AddonPanel = ({ globalState, dispatch, }) => {
    if (Object.keys(config.addons).every((addonName) => config.addons[addonName].enabled === false)) {
        return null;
    }
    return (_jsx("header", { role: "banner", className: "ladle-addons", children: _jsxs("ul", { children: [config.addons.control.enabled &&
                    Object.keys(globalState.control).length > 0 && (_jsx(ControlButton, { globalState: globalState, dispatch: dispatch })), config.addons.theme.enabled && (_jsx(ThemeButton, { globalState: globalState, dispatch: dispatch })), config.addons.mode.enabled && (_jsx(ModeButton, { globalState: globalState, dispatch: dispatch })), config.addons.width.enabled && (_jsx(WidthButton, { globalState: globalState, dispatch: dispatch })), config.addons.rtl.enabled && (_jsx(RtlButton, { globalState: globalState, dispatch: dispatch })), config.addons.source.enabled && (_jsx(SourceButton, { globalState: globalState, dispatch: dispatch })), config.addons.a11y.enabled && (_jsx(A11yButton, { globalState: globalState, dispatch: dispatch })), config.addons.ladle.enabled && (_jsx(LadleButton, { globalState: globalState, dispatch: dispatch })), config.addons.control.enabled && globalState.action.length > 0 && (_jsx(ActionButton, { globalState: globalState, dispatch: dispatch }))] }) }));
};
export default AddonPanel;
